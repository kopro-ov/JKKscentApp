import * as React from 'react';
import Ionicons from 'react-native-vector-icons/Ionicons';

function  IconCustom({ name }) {
    return (
        <Ionicons name={name} color='#ffffff' size={20}/>
    );
};

export default IconCustom;